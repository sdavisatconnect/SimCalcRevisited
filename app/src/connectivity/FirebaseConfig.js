/**
 * Firebase Realtime Database configuration.
 * REST API only — no SDK, no persistent WebSocket connections.
 */
export const FIREBASE_CONFIG = {
  databaseURL: 'https://simcalcrevisited-default-rtdb.firebaseio.com',
};
